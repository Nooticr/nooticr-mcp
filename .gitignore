node_modules/
dist/
*.log
.DS_Store
credentials.json
.env

cloudflare/.wrangler/
cloudflare/.dev.vars

# Playwright
test-results/
playwright-report/

# Agentic E2E evals (scripts/run-agentic-evals.sh) — generated per run,
# carry a live token and API key, never committed.
.mcpjam/environment.generated.json
.mcpjam/llms.json

# Quest runs (scripts/run-quests.sh) — transcripts, the generated MCP config
# (it carries a live dev-login token), and the report of the last run. The
# corpus in quests/quests.json is the committed part; a run's output is not.
quests/report/
